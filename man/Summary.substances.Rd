% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summary.R
\name{Summary.substances}
\alias{Summary.substances}
\alias{mean.substances}
\alias{median.substances}
\alias{quantile.substances}
\alias{weighted.mean.substances}
\title{Summaries of a substance vector}
\usage{
\method{Summary}{substances}(..., na.rm = FALSE)

\method{mean}{substances}(x, ...)

\method{median}{substances}(x, ...)

\method{quantile}{substances}(x, ...)

\method{weighted.mean}{substances}(x, w, ...)
}
\arguments{
\item{na.rm}{Passed to the underlying function.}

\item{x, ...}{\link{substances} vectors.}

\item{w}{Weights, as in \code{\link[stats:weighted.mean]{stats::weighted.mean()}}.}
}
\value{
A \code{substances} vector of length one, or of \code{length(probs)} for
\code{quantile()}.
}
\description{
\code{sum()}, \code{min()}, \code{max()}, \code{range()}, \code{mean()}, \code{median()}, \code{quantile()} and
\code{weighted.mean()} collapse many elements into one, which only means something
if every element is the same substance. An unknown substance is refused
rather than dropped: it is not evidence that the rest are alike.
}
\examples{
x <- set_substances(c(100, 140, 90), "glucose", "mg/dL")
sum(x)
mean(x)
range(x)
}
