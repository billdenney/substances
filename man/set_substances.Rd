% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/substances.R
\name{set_substances}
\alias{set_substances}
\alias{substances<-}
\alias{substances}
\alias{substance_system_of}
\alias{new_substances}
\title{Substance-aware quantities}
\usage{
set_substances(x = double(), value = NA_character_, unit = NULL, system = NULL)

substances(x) <- value

substances(x)

substance_system_of(x)

new_substances(
  x = double(),
  substance_id = character(),
  unit_sym = units::unitless,
  system_name = substance_default_system()
)
}
\arguments{
\item{x}{A numeric vector, a \link[units:units]{units::units} vector, or a \code{substances} vector.}

\item{value}{A character vector of substance names, synonyms or identifiers,
recycled to the length of \code{x}. \code{NA} means the substance is unknown, which
permits dimensional conversion but not parametric.}

\item{unit}{A unit, as a string, a \link[units:units]{units::units} object or \code{symbolic_units}.
Applied before the substance. If \code{x} has no unit yet this labels it; if it
already has one, this converts to \code{unit}.}

\item{system}{A conversion system name or object; defaults to
\code{\link[=substance_default_system]{substance_default_system()}}, or to \code{x}'s own system if it has one.}

\item{substance_id, unit_sym, system_name}{Low-level constructor arguments; no
checking is done.}
}
\value{
A \code{substances} vector, of class \code{c("substances", "units")}.
}
\description{
A vector of measurements that each carry the substance they measure. The
object is a \link[units:units]{units::units} vector with one extra attribute -- the substance
of every element -- so everything \code{units} can do it can do, and the methods
in this package keep the substance aligned with the values.
}
\details{
A different substance per element is the ordinary case, not an edge case:
long-format laboratory data has one analyte per row. Use
\code{\link[=mixed_substances]{mixed_substances()}} when the \emph{unit} also varies per element.
}
\examples{
set_substances(c(100, 140), "glucose", "mg/dL")
set_substances(c(100, 140), c("glucose", "sodium"), "mg/dL")

x <- units::set_units(c(100, 140), "mg/dL")
substances(x) <- c("glucose", "sodium")
x
}
