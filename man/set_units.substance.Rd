% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/convert.R
\name{set_units.substance}
\alias{set_units.substance}
\title{Convert a substance vector to another unit}
\usage{
\method{set_units}{substance}(x, value, ..., mode = units::units_options("set_units_mode"))
}
\arguments{
\item{x}{A \link{substance} vector.}

\item{value}{A unit, as in \code{\link[units:set_units]{units::set_units()}}.}

\item{...}{Passed to \code{\link[units:as_units]{units::as_units()}}.}

\item{mode}{If \code{"symbols"} (the default, following \code{units}) \code{value} is taken
unevaluated; if \code{"standard"} it is evaluated.}
}
\value{
A \code{substance} vector in the new unit.
}
\description{
Conversion is attempted in three stages: an explicit conversion registered
for that substance and unit pair; then an ordinary dimensional conversion by
\code{\link[units:set_units]{units::set_units()}}; then a bridge built from the substance's parameters
(molar mass, density, valence, activity). Explicit conversions are tried
first because some are between dimensionally-equivalent units.
}
\details{
Elements of different substances are converted independently, so a vector
holding several analytes converts in one call.
}
\examples{
x <- substance(c(100, 140), "mg/dL", c("glucose", "sodium"))
set_units(x, "mmol/L")
}
