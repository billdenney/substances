% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/substance.R
\name{substance}
\alias{substance}
\alias{new_substance}
\title{Substance-aware quantities}
\usage{
substance(
  x = double(),
  unit = units::unitless,
  substance = NA_character_,
  system = NULL
)

new_substance(
  value = double(),
  substance_id = character(),
  unit_sym = units::unitless,
  system_name = substance_default_system()
)
}
\arguments{
\item{x}{A numeric vector.}

\item{unit}{A unit, as a string, a \link[units:units]{units::units} object or \code{symbolic_units}.}

\item{substance}{A character vector of substance names, synonyms or
identifiers, recycled to the length of \code{x}. \code{NA} means the substance is
unknown, which permits dimensional conversion but not parametric.}

\item{system}{A conversion system name or object; defaults to
\code{\link[=substance_default_system]{substance_default_system()}}.}

\item{value, substance_id, unit_sym, system_name}{Low-level constructor
arguments; no checking is done.}
}
\value{
A \code{substance} vector.
}
\description{
A vector of measurements that each carry the substance they measure. The
substance is a \emph{field}, so it survives subsetting, concatenation, reordering
and data-frame operations; the unit is a single attribute for the whole
vector, exactly as in \link[units:units]{units::units}.
}
\details{
A different substance per element is the ordinary case, not an edge case:
long-format laboratory data has one analyte per row. Use
\code{\link[=mixed_substances]{mixed_substances()}} when the \emph{unit} also varies per element.
}
\examples{
substance(c(100, 140), "mg/dL", "glucose")
substance(c(100, 140), "mg/dL", c("glucose", "sodium"))
}
