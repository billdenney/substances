% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/arith.R
\name{substance_scale}
\alias{substance_scale}
\title{Multiply a substance quantity by a units quantity}
\usage{
substance_scale(x, by)
}
\arguments{
\item{x}{A \link{substance} vector.}

\item{by}{A \link[units:units]{units::units} quantity, or a plain number.}
}
\value{
A \code{substance} vector with the combined unit.
}
\description{
Concentration times volume is an amount, still of the same substance. This
needs its own function because \code{*} cannot be made to work: R refuses to
dispatch a binary operator when both operands carry methods from different
classes, so \code{x * units::set_units(3, "L")} fails with "Incompatible methods"
before any method of ours is reached. No S3 arrangement avoids that -- the
only thing that does is inheriting from \code{units}, which silently returns a
plain \code{units} object with the substance dropped, and is precisely what this
class exists to prevent.
}
\details{
Multiplying by a plain number uses \code{*} as usual; only \code{units} quantities
need this function. For division, multiply by the reciprocal.
}
\examples{
conc <- substance(2, "mmol/L", "glucose")
substance_scale(conc, units::set_units(3, "L"))       # -> mmol of glucose
substance_scale(conc, 1 / units::set_units(3, "L"))   # divide by a volume
}
