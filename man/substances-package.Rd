% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/substances-package.R
\docType{package}
\name{substances-package}
\alias{substances-package}
\title{substances: Substance-Aware Unit Conversions}
\description{
Unit conversions that depend on a property of the substance being measured. The 'units' package converts between commensurable units, but cannot convert mg/dL to mmol/L because that requires the analyte's molar mass. This package attaches a substance to each element of a vector, supplies the substance-specific quantities (molar mass, density, valence, biological activity) that bridge otherwise incommensurable dimensions, and refuses to convert or combine quantities of different substances unless the relationship is explicitly defined. Reference data ships as CSV with a citation for every value.
}
\details{
The \code{units} package converts between commensurable units. It cannot convert
mg/dL to mmol/L, because that needs the analyte's molar mass, and a unit
string has nowhere to put one.

The substance cannot live in the unit string either. Defining \code{mol_glucose}
in udunits relative to \code{mol} makes glucose and sodium mutually convertible,
so \verb{1 mol_glucose + 1 mol_sodium} silently returns a number; defining them as
base units instead keeps them apart but makes \code{mol} to \code{g} conversion
impossible. Isolation and conversion are mutually exclusive inside udunits.
So here the substance is an attribute of the vector, carried alongside the
unit, and the registry supplies the quantities that bridge dimensions \code{units}
cannot relate on its own.
}
\section{Object model}{

A \link{substances} vector is a \link[units:units]{units::units} vector with one extra attribute:
the substance of every element. Inheriting from \code{units} means every \code{units}
method works, and the methods in this package -- \code{[}, \code{c()}, \code{rep()}, \code{Ops},
\code{Summary} and the rest -- keep the substance aligned with the values.
\code{\link[=substances]{substances()}} checks that alignment on every read, so an operation this
package has not anticipated fails loudly instead of matching values to
analytes at random.

\code{vctrs} and \code{pillar} are supported but not required. Their methods are
registered at load time when those packages are present, which is what lets
\code{vec_slice()}, \code{vec_c()} and \code{dplyr::bind_rows()} carry the substance too.
}

\section{R version}{

R >= 4.3.0 is required for \code{\link[=chooseOpsMethod]{chooseOpsMethod()}}, which is what lets a
\code{substances} vector interoperate with a plain \link[units:units]{units::units} quantity.
Without it R refuses to choose between the two classes' operator methods:
\code{x * units::set_units(3, "L")} warns "Incompatible methods" and returns the
values with \code{x}'s unit unchanged -- a wrong answer rather than an error.
}

\section{Out of scope}{

Normalising unit \emph{strings} (\code{ng/ml} versus \code{ng/mL}, \code{IU/L} versus \code{U/L}) is
per-source data cleaning and belongs in the consuming package. So are
conversions \code{units} already performs: \code{mg/dL} to \code{g/L} is dimensional, and so
is \code{U/L} to \code{ukat/L} once \code{U} is defined as \code{umol/min}. This package handles
only conversions that need a property of the substance.
}

\seealso{
Useful links:
\itemize{
  \item \url{https://github.com/r-quantities/substances}
  \item Report bugs at \url{https://github.com/r-quantities/substances/issues}
}

}
\author{
\strong{Maintainer}: Bill Denney \email{wdenney@humanpredictions.com} (\href{https://orcid.org/0000-0002-5759-428X}{ORCID}) [copyright holder]

Authors:
\itemize{
  \item Bill Denney \email{wdenney@humanpredictions.com} (\href{https://orcid.org/0000-0002-5759-428X}{ORCID}) [copyright holder]
}

}
\keyword{internal}
